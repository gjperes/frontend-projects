import React, {Component} from 'react';

class RollButton extends Component {
    render() {
        return (
          <button>Clicai-vos</button>
        );
    }
}

export default RollButton;
